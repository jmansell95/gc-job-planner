{
  "name": "Job",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Job name"
    },
    "job_reference": {
      "type": "string",
      "description": "Internal job reference or PO number"
    },
    "location": {
      "type": "string",
      "description": "Site location/address"
    },
    "job_type": {
      "type": "string",
      "enum": [
        "groundworks",
        "cp_drilling",
        "rotary_drilling",
        "enabling_works",
        "depot"
      ],
      "description": "Legacy job type \u2014 no longer used for assignment restrictions. Kept for backward-compatible display on jobs created before the team-centric migration."
    },
    "required_team_ids": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Teams required for this job. Staff outside these teams can still be assigned but managers see a soft warning. Derived from the legacy job_type during migration."
    },
    "status": {
      "type": "string",
      "enum": [
        "planning",
        "in_progress",
        "completed",
        "on_hold"
      ],
      "default": "planning",
      "description": "Job status"
    },
    "start_date": {
      "type": "string",
      "format": "date",
      "description": "Job start date"
    },
    "end_date": {
      "type": "string",
      "format": "date",
      "description": "Job end date"
    },
    "client_id": {
      "type": "string",
      "description": "Associated client"
    },
    "contractor_id": {
      "type": "string",
      "description": "Associated contractor"
    },
    "project_manager": {
      "type": "string",
      "description": "Project manager responsible for the job"
    },
    "site_contact_name": {
      "type": "string",
      "description": "On-site contact name"
    },
    "site_contact_phone": {
      "type": "string",
      "description": "On-site contact phone number"
    },
    "notes": {
      "type": "string",
      "description": "Job notes and details"
    },
    "requisition_list_url": {
      "type": "string",
      "description": "URL of the uploaded requisition/equipment list file"
    },
    "requisition_list_name": {
      "type": "string",
      "description": "Original filename of the requisition list"
    },
    "portal_token": {
      "type": "string",
      "description": "Unique token for client portal access"
    },
    "portal_enabled": {
      "type": "boolean",
      "default": false,
      "description": "Whether client portal access is enabled for this job"
    },
    "portal_sections": {
      "type": "object",
      "description": "Visibility flags for each section shown on the client portal",
      "properties": {
        "progress": {
          "type": "boolean",
          "default": true
        },
        "client_info": {
          "type": "boolean",
          "default": true
        },
        "schedule": {
          "type": "boolean",
          "default": true
        },
        "notes": {
          "type": "boolean",
          "default": true
        },
        "photos": {
          "type": "boolean",
          "default": true
        },
        "milestones": {
          "type": "boolean",
          "default": true
        },
        "documents": {
          "type": "boolean",
          "default": true
        },
        "comments": {
          "type": "boolean",
          "default": true
        },
        "team": {
          "type": "boolean",
          "default": true
        },
        "client_charge": {
          "type": "boolean",
          "default": false
        }
      }
    },
    "budget_amount": {
      "type": "number",
      "description": "Job budget in GBP for cost tracking"
    },
    "actual_cost": {
      "type": "number",
      "description": "Manually recorded actual cost in GBP; overrides the auto-calculated labour cost when set"
    },
    "meterage": {
      "type": "number",
      "description": "Total meters drilled for drilling jobs (CP / rotary); overrides shift-level meterage for costing when set"
    },
    "markup_percentage": {
      "type": "number",
      "default": 0,
      "description": "Markup percentage applied to total internal net cost to derive the client price"
    },
    "vat_rate": {
      "type": "number",
      "default": 20,
      "description": "VAT rate (%) applied for billing calculations. UK standard rate is 20."
    },
    "client_charge": {
      "type": "number",
      "description": "Legacy manual client charge. Superseded by the calculated billing when cost items/markup are used."
    },
    "client_charge_description": {
      "type": "string",
      "description": "Label shown alongside the client billing total on the portal (e.g. 'Agreed quote', 'Project Investment')"
    }
  },
  "required": [
    "name",
    "location",
    "start_date",
    "end_date"
  ]
}