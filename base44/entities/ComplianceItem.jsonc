{
  "name": "ComplianceItem",
  "type": "object",
  "properties": {
    "category": {
      "type": "string",
      "enum": [
        "staff",
        "vehicle",
        "job",
        "company",
        "equipment"
      ],
      "description": "What this compliance item relates to"
    },
    "title": {
      "type": "string",
      "description": "Name of the compliance item (e.g. CSCS Card, CPCS Card, Vehicle MOT, Public Liability Insurance, RAMS)"
    },
    "qualification_type": {
      "type": "string",
      "enum": [
        "cscs_card",
        "cpcs_card",
        "npors_card",
        "first_aid_cert",
        "driver_license",
        "dbs_certificate",
        "other"
      ],
      "default": "other",
      "description": "Standardized qualification type for filtering, badges, and special UI (e.g. CSCS front/back card display)"
    },
    "reference_name": {
      "type": "string",
      "description": "Name of the person, vehicle, job or entity this item belongs to"
    },
    "reference_id": {
      "type": "string",
      "description": "Optional linked entity ID (staff, vehicle, job)"
    },
    "card_number": {
      "type": "string",
      "description": "Card or registration number (e.g. CSCS registration number)"
    },
    "issue_date": {
      "type": "string",
      "format": "date",
      "description": "Date issued or last renewed"
    },
    "expiry_date": {
      "type": "string",
      "format": "date",
      "description": "When this compliance item expires"
    },
    "document_url": {
      "type": "string",
      "description": "URL of the uploaded certificate or front of card"
    },
    "document_name": {
      "type": "string",
      "description": "Original filename of the uploaded document"
    },
    "back_document_url": {
      "type": "string",
      "description": "URL of the back of the card (e.g. CSCS card back side)"
    },
    "back_document_name": {
      "type": "string",
      "description": "Original filename of the back document"
    },
    "responsible_person": {
      "type": "string",
      "description": "Who is responsible for maintaining this compliance item"
    },
    "notes": {
      "type": "string",
      "description": "Optional notes"
    },
    "status_override": {
      "type": "string",
      "enum": [
        "auto",
        "missing",
        "not_required"
      ],
      "default": "auto",
      "description": "Override the auto-calculated status. 'auto' derives from expiry_date, 'missing' marks it as not yet provided, 'not_required' exempts it."
    }
  },
  "required": [
    "category",
    "title"
  ]
}